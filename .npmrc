# Speed up npm installs
prefer-offline=true
audit=false
fund=false
progress=false

# Use faster registry mirror (optional)
# registry=https://registry.npmjs.org/

# Enable package-lock
package-lock=true

# Reduce network timeouts for faster fails
fetch-retry-mintimeout=2000
fetch-retry-maxtimeout=10000
fetch-retries=2